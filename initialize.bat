@echo off

echo # initialize
echo.
echo # Installation
echo 1. Open your terminal and clone the `initialize`.
echo 2. Install all the necessary dependencies using `npm run initialize:install`.
echo 3. Run `npm run initialize:dev` and your done! Congratulations, you're now using `initialize`.
echo.
echo.
echo # Commands
echo To use the commands simply type `npm run` followed by the command. i.e. `npm run initialize:help`.
echo 1. initialize:install      :   install dependencies.
echo 2. initialize:dev          :   start initialize in `development` mode.
echo 3. initialize:serve        :   start initialize in `serve` mode. (initialize with PHP!)
echo - You can now use initialize with PHP,  just include `initialize` inside your `htdocs` (XAMPP) or `www` (WAMP)
echo 4. initialize:prod         :   create minified files for both `JS` and `CSS`.
echo 5. initialize:optimize     :   optimize images `PNG, JPG, SVG`.
echo 6. initialize:help         :   display commands.
echo.
echo.
echo Developed with heart by Art